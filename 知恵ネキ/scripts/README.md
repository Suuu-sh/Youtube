# Scripts Placeholder

今後ここに置く想定:

- `generate_script.*` ネタから台本を作る
- `generate_metadata.*` タイトル/説明文案を作る
- `render_short.*` 縦動画を書き出す
- `upload_private.*` YouTube に private でアップロードする

最初はコードより先に、

1. `ideas/shorts_ideas.csv`
2. `prompts/short_script_prompt.md`
3. `metadata/shorts_upload_template.md`

を使って、1本の投稿フローを手で通す。
